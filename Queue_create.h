#include <string>

#ifndef Queue_create_H
#define Queue_create_H


int random_Int(int min, int max);

float random_Float(float min, float max);

double random_Double(double min, double max);

char random_Char();

bool random_Bool();

std::string create_queue(int index);

#endif